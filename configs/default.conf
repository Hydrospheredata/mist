mist {
  context-defaults.spark-conf = {
    spark.master = "local[*]"
  }

  http {
    on = true
    host = "localhost"
    port = 2004
    router-config-path = "configs/router.conf"
  }

  workers.runner = "local"

  mqtt.on = false
  recovery.on = false
  context-defaults.disposable = false
}