mist {
  context-defaults.spark-conf = {
    spark.master = "local[*]"
  }

  cluster.host = "auto"
  http.host = "auto"
  log-service.host = "auto"

  workers {
    runner = "docker"
    docker {
      image = "hydrosphere/mist:1.0.0-RC13-2.3.0"
      network-type = "auto-master"
    }
  }
}
